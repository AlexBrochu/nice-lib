const EventType = {
  Registration: 'Registration',
  LevelComplete: 'LevelComplete',
  Purchase: 'Purchase',
};

const Test = {
  compute(st: string): string {
    return st + '*************';
  },
};

export {EventType, Test};
